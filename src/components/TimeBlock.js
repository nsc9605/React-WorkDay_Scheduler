import React from "react";
import '../styles/TimeBlock';

function TimeBlock(props) {
    return( 
    <div className="row">
        <form>
            {/* <label className="col-sm-1 hour" id={hours.id}>{props.hour}</label> */}
        </form>
    </div>
  )
  }


export default TimeBlock;
